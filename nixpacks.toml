[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
# Temporalmente omitido db:push para resolver problema de migración con duplicados
# cmds = ["pnpm install && pnpm db:push && pnpm build"]
cmds = ["pnpm build"]

[start]
cmd = "pnpm start"
